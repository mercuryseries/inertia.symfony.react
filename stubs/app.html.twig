<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title inertia>[TO_REPLACE]</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        {{ inertiaHead(page) }}
    </head>
    <body>
        {{ inertia(page) }}
        {% block body %}{% endblock %}
    </body>
</html>
